version: '3.8'

services:
  asta-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: asta-react-app
    ports:
      - "11000:80"
    restart: unless-stopped
    networks:
      - asta-network

networks:
  asta-network:
    driver: bridge

